# Dependencies and build output (rebuilt inside the image)
node_modules
build
.svelte-kit
.output
.vercel
.netlify
.wrangler

# Secrets — never send to Docker
.env
.env.*
!.env.example

# VCS and CI (not needed to build)
.git
.github

# Dev / docs / tooling
docs
scripts
README.md
docker-compose.yaml
Dockerfile
.cursor
.vscode
.kilo

# OS / editor junk
.DS_Store
Thumbs.db
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
