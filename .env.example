# Host port for Docker (default: 3000)
APP_PORT=3000

# Environment (development, production, etc.)
VITE_ENV=development

# Enable debug overlay in the UI ("true" or "false")
PUBLIC_DEBUG=false

# Sentry authentication token for release tracking in CI (optional for local dev)
SENTRY_AUTH_TOKEN=
