# Host port for Docker (default: 3000)
APP_PORT=3000

# Environment label (local, staging, production, etc.)
PUBLIC_ENV=local

# Enable debug overlay in the UI ("true" or "false")
PUBLIC_DEBUG=false

# Sentry DSN (leave empty to disable error reporting)
PUBLIC_SENTRY_DSN=
