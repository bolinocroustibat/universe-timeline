/** Viewport span above which geological periods are hidden (cosmic scale). */
export const GEOLOGICAL_PERIODS_MAX_VIEWPORT_SPAN_YEARS = 8_000_000_000

/** Default opacity for geological period bands in background mode (0–1). */
export const GEOLOGICAL_PERIOD_BACKGROUND_OPACITY = 0.38

/** Opacity when a background band is hovered (0–1). */
export const GEOLOGICAL_PERIOD_HOVER_OPACITY = 0.58

/** Opacity when a background band is selected (0–1). */
export const GEOLOGICAL_PERIOD_SELECTED_OPACITY = 0.72
